const initialState = {
  user: {},
};

export default function auth(state = initialState, action: any) {
  return state;
  //   switch (action.type) {
  //     case TYPES.GET_NOTIFICATION_LIST:
  //       return {
  //         ...state,
  //       };
  //     default:
  //       return state;
  //   }
}
